<Window x:Class="SliderBindDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SliderBindDemo"
        mc:Ignorable="d"
        Title="SliderDemo" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>
        <Slider
            Name="sldTick"
            Minimum="1" 
            Maximum="1000" 
            TickFrequency="10"
            IsSnapToTickEnabled="True"
            TickPlacement="BottomRight"
            Margin="10"
            Background="MidnightBlue"></Slider>
        <StackPanel Grid.Row="1">
            <TextBox  
                Margin="10" 
                Text="{Binding Path=Value, ElementName=sldTick, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                
            </TextBox>
        </StackPanel>
    </Grid>
</Window>
